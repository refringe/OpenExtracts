const ORIGINAL_COOP_FENCE_REP_GAIN = 0.25;
const COOP_EXTRACT_FENCE_REP_MULTIPLIER = 0.5;

export function adjustFenceRep(): void {}

function calculateNewFenceRep(): number {
    return 0;
}

function getFenceRep(): number {
    return 0;
}

function getExtractCount(): number {
    return 0;
}
